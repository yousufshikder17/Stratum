{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "universe_csv adapter configuration",
  "type": "object",
  "properties": {
    "membership_path": {
      "type": "string",
      "description": "Effective-dated membership intervals: security, added_on, announced_on, and removed_on with removed_announced_on for names that left. A flat current-constituents list is rejected."
    },
    "universe_id": {
      "type": "string",
      "description": "Identifier this membership history defines, e.g. sp1500_pit."
    },
    "source_id": {
      "type": "string",
      "description": "Provider id recorded on every observation. Defaults to the adapter id."
    }
  },
  "required": ["membership_path", "universe_id"],
  "additionalProperties": false
}
